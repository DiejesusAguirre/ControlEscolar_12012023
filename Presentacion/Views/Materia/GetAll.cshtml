@model Negocio.Materia
@{
    ViewBag.Title = "GetAll";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h2>Materias</h2>

<div class="container">
    <p>Aqui puede ver, editar o agrgar a una Materia</p>
    <p>Para agregar a una Materia presione el boton verde</p>
    <p>Para editar a una Materia presione el boton amarillo</p>
    <p>Para eliminar una Materia presione el boton rojo</p>

    <div class="row">
        <div style="float:right">
            @Html.ActionLink("Añadir +", "Form", "Materia", htmlAttributes: new { @class = "btn btn-success" })
        </div>
    </div>

    <div class="row">

        <table class="table table-hover" id="tblMaterias">
            <thead>
                <tr>
                    <th scope="col">Editar</th>
                    <th style="display: none">ID Materia</th>
                    <th scope="col">Nombre de la Materia</th>
                    <th scope="col">Costo</th>
                    <th scope="col">Eliminar</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Negocio.Materia materia in Model.Materias)
                {
                    <tr>
                        <td class="text-center"><a class="btn btn-warning" href="@Url.Action("Form", "Materia", new { IdMateria = materia.IdMateria })"><i class="bi bi-arrow-repeat" style="color: mintcream"></i></a></td>
                        <td style="display: none">@materia.IdMateria</td>
                        <td>@materia.Nombre</td>
                        <td>@materia.Costo</td>
                        <td class="text-center"><a class="btn btn-danger" href="@Url.Action("Delete", "Materia", new { IdMateria = materia.IdMateria })" onclick="return confirm('Estas seguro que deseas eliminar este registro?');"><i class="bi bi-eraser-fill" style="color: mintcream"></i></a></td>

                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>

@section Scripts
    {

    <script src="https://code.jquery.com/jquery-3.5.1.js"></script>
    <script src="https://cdn.datatables.net/1.13.1/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.1/js/dataTables.material.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.1/js/dataTables.bootstrap5.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#tblMaterias').DataTable({
                searching: true,
                "lengthMenu": [[5, 10, 15, 20, -1], [5, 10, 15, 20, "All"]],
                autoWidth: false,
                columnDefs: [
                    {
                        targets: ['_all'],
                        className: 'mdc-data-table__cell',
                    },
                ],
                language: {
                    url: "//cdn.datatables.net/plug-ins/9dcbecd42ad/i18n/Spanish.json",
                },
            });
        });


    </script>

}
